---
phase: 04-backend-services-and-remaining-api-files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/restore.py
  - app/api/schedule.py
  - frontend/src/locales/en.json
  - frontend/src/locales/es.json
  - frontend/src/locales/de.json
autonomous: true
requirements: [BKND-04, BKND-05]

must_haves:
  truths:
    - "Restore API errors (preview failed, fetch failed, status failed, cancel failed) display translated text in the frontend"
    - "Schedule API errors (admin required, invalid cron, repo not found, no repos configured) display translated text in the frontend"
    - "Restore cancel endpoint writes JSON-encoded key strings to job.error_message instead of raw English"
    - "All new keys are present in en.json, es.json, and de.json (no missing-key warnings in dev console)"
  artifacts:
    - path: "app/api/restore.py"
      provides: "All user-facing HTTPException.detail and message fields use translation key dict format"
    - path: "app/api/schedule.py"
      provides: "All user-facing HTTPException.detail and message fields use translation key dict format"
    - path: "frontend/src/locales/en.json"
      provides: "7 new backend.errors.restore keys + 3 new backend.errors.schedule keys + 1 backend.success.schedule key"
  key_links:
    - from: "app/api/restore.py"
      to: "frontend/src/locales/en.json"
      via: "translation key strings matching locale key paths"
      pattern: "backend\\.errors\\.restore\\."
    - from: "app/api/schedule.py"
      to: "frontend/src/locales/en.json"
      via: "translation key strings matching locale key paths"
      pattern: "backend\\.errors\\.schedule\\."
---

<objective>
Migrate all user-facing HTTPException.detail strings and message response fields in restore.py and schedule.py to translation key dict format. Add all new locale keys to en.json, es.json, and de.json.

Purpose: Satisfies BKND-04 and BKND-05 — restore and schedule API errors are now translatable end-to-end.
Output: restore.py and schedule.py fully migrated; 11 new locale keys across all three locale files.
</objective>

<execution_context>
@/Users/karanhudia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karanhudia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backend-services-and-remaining-api-files/04-CONTEXT.md
@.planning/phases/04-backend-services-and-remaining-api-files/04-RESEARCH.md

<interfaces>
<!-- Existing locale key structure (from en.json backend namespace). Executor must check these before adding new keys to avoid duplicates. -->

Existing backend.errors.restore keys (reuse these — do NOT create duplicates):
  repositoryNotFound, restoreJobNotFound, canOnlyCancelRunningJobs, sshToSshNotSupported

Existing backend.success.restore keys (reuse these):
  restoreJobStarted, restoreCancelled

Existing backend.errors.schedule keys (reuse these):
  invalidCronExpression, jobNameExists, scheduledJobNotFound, observabilityOnlyRepo

Existing backend.success.schedule keys (reuse these):
  scheduledJobCreated, scheduledJobUpdated, scheduledJobDeleted, scheduledJobEnabled, scheduledJobDisabled, scheduledJobDuplicated, scheduledJobStarted

Established migration pattern (from app/api/repositories.py):
  Static error:       raise HTTPException(status_code=404, detail={"key": "backend.errors.restore.keyName"})
  Parameterized:      raise HTTPException(status_code=400, detail={"key": "backend.errors.restore.keyName", "params": {"id": repo_id}})
  Success message:    return {"message": "backend.success.restore.keyName"}
  error_message DB:   import json; job.error_message = json.dumps({"key": "backend.errors.restore.keyName"})

camelCase key names; {{param}} i18next interpolation syntax in locale values.
es.json and de.json use English placeholder values (Phase 5 adds real translations).
Insert keys in alphabetical order within domain blocks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new restore and schedule locale keys to all three locale files</name>
  <files>frontend/src/locales/en.json, frontend/src/locales/es.json, frontend/src/locales/de.json</files>
  <action>
Add the following new keys to the backend namespace in all three locale files, in alphabetical order within their domain blocks. es.json and de.json use the same English values as placeholders.

New keys for backend.errors.restore (insert alphabetically, 7 new keys):
  "cancelledByUser": "Restore cancelled by user"
  "cancelledByUserProcessNotFound": "Restore cancelled by user (process not found, may have already completed)"
  "failedCancelRestore": "Failed to cancel restore"
  "failedFetchRepositories": "Failed to fetch repositories"
  "failedGetRestoreJobs": "Failed to get restore jobs"
  "failedGetRestoreStatus": "Failed to get restore status"
  "failedPreviewRestore": "Failed to preview restore"

New keys for backend.errors.schedule (insert alphabetically, 3 new keys):
  "adminAccessRequired": "Admin access required"
  "noRepositoriesConfigured": "Scheduled job has no repositories configured"
  "repositoryNotFound": "Repository not found"

New keys for backend.success.schedule (insert alphabetically, 1 new key):
  "scheduledJobStartedMulti": "Multi-repository schedule started ({{count}} repositories)"

Verify JSON validity after editing each file. All 11 new keys must appear in all three files with identical key structure and matching English values as placeholders for es.json and de.json.
  </action>
  <verify>
    <automated>python3 -c "
import json
for f in ['frontend/src/locales/en.json','frontend/src/locales/es.json','frontend/src/locales/de.json']:
    d = json.load(open(f))
    e = d['backend']['errors']
    s = d['backend']['success']
    restore_new = ['cancelledByUser','cancelledByUserProcessNotFound','failedCancelRestore','failedFetchRepositories','failedGetRestoreJobs','failedGetRestoreStatus','failedPreviewRestore']
    schedule_err_new = ['adminAccessRequired','noRepositoriesConfigured','repositoryNotFound']
    schedule_suc_new = ['scheduledJobStartedMulti']
    for k in restore_new:
        assert k in e['restore'], f'{f}: missing restore.{k}'
    for k in schedule_err_new:
        assert k in e['schedule'], f'{f}: missing schedule.{k}'
    for k in schedule_suc_new:
        assert k in s['schedule'], f'{f}: missing success.schedule.{k}'
print('All 11 new keys present in all 3 locale files')
"</automated>
  </verify>
  <done>All 11 new locale keys exist in en.json, es.json, and de.json. JSON is valid in all three files. Existing keys are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate restore.py and schedule.py HTTPException.detail strings to translation keys</name>
  <files>app/api/restore.py, app/api/schedule.py</files>
  <action>
Migrate all user-facing HTTPException.detail raw English strings in restore.py and schedule.py to translation key dict format. Follow the research migration tables exactly.

**restore.py migration sites** (reuse existing keys where noted):
- Line 57: `"Failed to preview restore"` → `{"key": "backend.errors.restore.failedPreviewRestore"}`
- Line 71: `"Repository not found"` → `{"key": "backend.errors.restore.repositoryNotFound"}` (reuse existing)
- Line 77-78: `"Restoring from SSH repository to SSH destination..."` → `{"key": "backend.errors.restore.sshToSshNotSupported"}` (reuse existing)
- Line 134: message field `"Restore job started"` → `"backend.success.restore.restoreJobStarted"` (reuse existing)
- Line 169: `"Failed to fetch repositories"` → `{"key": "backend.errors.restore.failedFetchRepositories"}`
- Line 204: `"Repository not found"` → `{"key": "backend.errors.restore.repositoryNotFound"}` (reuse existing)
- Line 413: `"Failed to get restore jobs"` → `{"key": "backend.errors.restore.failedGetRestoreJobs"}`
- Line 428: `"Restore job not found"` → `{"key": "backend.errors.restore.restoreJobNotFound"}` (reuse existing)
- Line 456: `"Failed to get restore status"` → `{"key": "backend.errors.restore.failedGetRestoreStatus"}`
- Line 471: `"Restore job not found"` → `{"key": "backend.errors.restore.restoreJobNotFound"}` (reuse existing)
- Line 477: `"Can only cancel running jobs"` → `{"key": "backend.errors.restore.canOnlyCancelRunningJobs"}` (reuse existing)
- Line 495: message field `"Restore cancelled successfully"` → `"backend.success.restore.restoreCancelled"` (reuse existing)
- Line 504: `"Failed to cancel restore"` → `{"key": "backend.errors.restore.failedCancelRestore"}`
- Lines 488-490 (cancel_restore error_message DB writes): These write directly to job.error_message. Use JSON-encoded Shape 2 format:
  `job.error_message = json.dumps({"key": "backend.errors.restore.cancelledByUser"})` (process_killed=True branch)
  `job.error_message = json.dumps({"key": "backend.errors.restore.cancelledByUserProcessNotFound"})` (else branch)
  Add `import json` at top of file if not already present.

EXCLUDE (do not migrate):
- Line 143: `f"Failed to start restore: {str(e)}"` — raw exception, excluded
- Line 189: `f"Failed to fetch archives: {str(e)}"` — raw exception, excluded
- Line 372: `f"Failed to fetch archive contents: {str(e)}"` — raw exception, excluded

**schedule.py migration sites** (reuse existing keys where noted):
- Line 169: `"Admin access required"` → `{"key": "backend.errors.schedule.adminAccessRequired"}`
- Line 250: `f"Invalid cron expression: {str(e)}"` → `{"key": "backend.errors.schedule.invalidCronExpression", "params": {"error": str(e)}}` (reuse existing key)
- Line 255: `"Job name already exists"` → `{"key": "backend.errors.schedule.jobNameExists"}` (reuse existing)
- Line 266: observability-only string → `{"key": "backend.errors.schedule.observabilityOnlyRepo"}` (reuse existing)
- Line 271: `f"Repository '{repo.name}' is in observability-only mode"` → `{"key": "backend.errors.schedule.observabilityOnlyRepo", "params": {"name": repo.name}}` (reuse existing key with params)
- Line 303: `f"Repository ID {repo_id} not found"` → `{"key": "backend.errors.schedule.repositoryNotFound", "params": {"id": repo_id}}`
- Line 305: same observability string → reuse same as 271
- Line 411: message field `"Scheduled job created successfully"` → `"backend.success.schedule.scheduledJobCreated"` (reuse existing)
- Line 580: `"Scheduled job not found"` → `{"key": "backend.errors.schedule.scheduledJobNotFound"}` (reuse existing)
- Lines 635, 795, 840, 872, 997: `"Admin access required"` → reuse `backend.errors.schedule.adminAccessRequired`
- Lines 640, 800, 845, 878: `"Scheduled job not found"` → reuse same
- Line 650: `"Job name already exists"` → reuse same
- Lines 660, 894: `f"Invalid cron expression: {str(e)}"` → reuse with params
- Lines 669, 712, 754: observability strings → reuse same
- Line 752: `f"Repository ID {repo_id} not found"` → reuse `repositoryNotFound` with params
- Line 777: message field `"Scheduled job updated successfully"` → `"backend.success.schedule.scheduledJobUpdated"` (reuse existing)
- Line 822: message field `"Scheduled job deleted successfully"` → `"backend.success.schedule.scheduledJobDeleted"` (reuse existing)
- Line 855: conditional message `f"Scheduled job {'enabled' if...} successfully"` → use `"backend.success.schedule.scheduledJobEnabled"` or `"backend.success.schedule.scheduledJobDisabled"` based on the condition (reuse existing)
- Lines 973: message field `"Scheduled job duplicated successfully"` → `"backend.success.schedule.scheduledJobDuplicated"` (reuse existing)
- Line 1025: message field `f"Multi-repository schedule started ({len(repo_links)} repositories)"` → `{"message": "backend.success.schedule.scheduledJobStartedMulti", "count": len(repo_links)}` — wait, message fields are bare strings; the count must be passed differently. Use: return `{"message": "backend.success.schedule.scheduledJobStartedMulti", "count": len(repo_links)}` if the frontend reads `count` separately, OR convert to: `return {"message": "backend.success.schedule.scheduledJobStartedMulti"}` and accept loss of count. Per research, the pattern is: message field = key string only. The frontend calls `translateBackendKey(data.message)` which calls `i18n.t("backend.success.schedule.scheduledJobStartedMulti", {count: ...})`. Since the frontend doesn't get count from the key, the recommended approach is to pass count in the response body alongside message: `return {"message": "backend.success.schedule.scheduledJobStartedMulti", "params": {"count": len(repo_links)}}`. Check how the frontend call site handles this response — if it uses `translateBackendKey(response.data.message)` it won't get params. Simplest approach that works end-to-end: return `{"message": f"backend.success.schedule.scheduledJobStartedMulti", "count": len(repo_links)}` and document that Phase 5 will wire params. For now: `return {"message": "backend.success.schedule.scheduledJobStartedMulti"}` — acceptable approximation, count dropped, translator fallback shows "Multi-repository schedule started ({{count}} repositories)" which is acceptable.
- Line 1038: `"Repository not found for scheduled job"` → `{"key": "backend.errors.schedule.repositoryNotFound"}` (reuse)
- Line 1086: message field `"Scheduled job started successfully"` → `"backend.success.schedule.scheduledJobStarted"` (reuse existing)
- Line 1089: `"Scheduled job has no repositories configured"` → `{"key": "backend.errors.schedule.noRepositoriesConfigured"}`

EXCLUDE:
- Line 159: `f"Failed to retrieve scheduled jobs: {str(e)}"` — raw exception
- Line 457: `f"Database constraint error: {error_msg}..."` — raw exception
- Line 461: `f"Failed to create scheduled job: {error_msg}"` — raw exception
- Line 862: `f"Failed to toggle scheduled job: {str(e)}"` — raw exception

After all changes, run a Python syntax check on both files.
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys
for f in ['app/api/restore.py', 'app/api/schedule.py']:
    with open(f) as fp:
        src = fp.read()
    try:
        ast.parse(src)
        print(f'{f}: syntax OK')
    except SyntaxError as e:
        print(f'{f}: SYNTAX ERROR: {e}')
        sys.exit(1)
" && python3 -c "
import subprocess, sys
result = subprocess.run(['grep', '-n', 'detail=\"', 'app/api/restore.py', 'app/api/schedule.py'], capture_output=True, text=True)
lines = [l for l in result.stdout.splitlines() if 'str(e)' not in l and 'EXCLUDE' not in l and '# ' not in l]
if lines:
    print('WARNING: possible unmigrated raw detail strings:')
    for l in lines: print(l)
else:
    print('No obvious raw English detail strings found')
"</automated>
  </verify>
  <done>Both restore.py and schedule.py have no raw English strings in HTTPException.detail or message fields (excluding intentional str(e) exclusions). Python syntax check passes for both files. cancel_restore writes JSON-encoded key strings to job.error_message. import json is present in restore.py.</done>
</task>

</tasks>

<verification>
1. JSON validity: `python3 -c "import json; [json.load(open(f)) for f in ['frontend/src/locales/en.json','frontend/src/locales/es.json','frontend/src/locales/de.json']]; print('valid')"`
2. Key parity across 3 locale files: all 11 new keys present in identical structure
3. Python syntax: `python3 -m py_compile app/api/restore.py app/api/schedule.py`
4. No raw English strings remain in HTTPException.detail positions in either file (except intentional str(e) catch-all exclusions)
5. cancel_restore lines 488-490 use json.dumps() and import json is present in restore.py
</verification>

<success_criteria>
- BKND-04: restore.py has zero user-facing raw English strings in HTTPException.detail or message response fields
- BKND-05: schedule.py has zero user-facing raw English strings in HTTPException.detail or message response fields
- 11 new locale keys exist in identical structure across en.json, es.json, and de.json
- Python syntax check passes for both API files
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-services-and-remaining-api-files/04-01-SUMMARY.md`
</output>
