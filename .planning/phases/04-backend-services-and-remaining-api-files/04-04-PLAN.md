---
phase: 04-backend-services-and-remaining-api-files
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - app/services/backup_service.py
  - app/services/restore_service.py
  - app/utils/process_utils.py
  - frontend/src/components/ErrorDetailsDialog.tsx
  - frontend/src/locales/en.json
  - frontend/src/locales/es.json
  - frontend/src/locales/de.json
autonomous: true
requirements: [SVC-01, SVC-02, SVC-03]

must_haves:
  truths:
    - "Stored error_message values in the database for backup and restore jobs use JSON-encoded key strings instead of raw English"
    - "ErrorDetailsDialog renders job.error_message through translateBackendKey line-by-line, displaying translated text"
    - "The LOCK_ERROR:: sentinel in error_message is preserved unchanged and BackupJobsTable lock detection still works"
    - "process_utils.py warning strings appear as translated text in ErrorDetailsDialog (not raw English)"
    - "format_error_message() borg CLI output continues to display as-is (Shape 4 raw fallback)"
    - "All new backend.errors.service keys are present in en.json, es.json, and de.json"
  artifacts:
    - path: "app/services/backup_service.py"
      provides: "error_message write sites use json.dumps({'key': 'backend.errors.service.*'}) format"
    - path: "app/services/restore_service.py"
      provides: "error_message write sites use json.dumps({'key': 'backend.errors.service.*'}) format"
    - path: "app/utils/process_utils.py"
      provides: "Container restart strings use json.dumps key format; warning appends use newline + json.dumps pattern"
    - path: "frontend/src/components/ErrorDetailsDialog.tsx"
      provides: "job.error_message split on newlines, each line passed through translateBackendKey()"
    - path: "frontend/src/locales/en.json"
      provides: "New backend.errors.service domain with all service layer keys"
  key_links:
    - from: "app/services/backup_service.py"
      to: "frontend/src/components/ErrorDetailsDialog.tsx"
      via: "json.dumps({'key': 'backend.errors.service.*'}) stored in DB, rendered via translateBackendKey"
      pattern: "backend\\.errors\\.service\\."
    - from: "app/utils/process_utils.py"
      to: "frontend/src/components/ErrorDetailsDialog.tsx"
      via: "warning appended as newline + JSON-encoded key, rendered line-by-line"
      pattern: "warningFailedBreakLock|warningRemoteProcessMayBeRunning"
    - from: "frontend/src/components/ErrorDetailsDialog.tsx"
      to: "frontend/src/utils/translateBackendKey.ts"
      via: ".split('\\n').map(line => translateBackendKey(line)).join('\\n')"
      pattern: "translateBackendKey"
---

<objective>
Migrate the services layer that writes stored error_message fields to use JSON-encoded translation key strings, and update ErrorDetailsDialog.tsx to translate those keys when rendering. Establishes the backend.errors.service locale domain.

Purpose: Satisfies SVC-01, SVC-02, SVC-03 — stored database error messages are now translatable, completing the full backend-to-frontend i18n chain.
Output: Three service files migrated; ErrorDetailsDialog updated; new service locale domain with 10 keys.
</objective>

<execution_context>
@/Users/karanhudia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karanhudia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backend-services-and-remaining-api-files/04-CONTEXT.md
@.planning/phases/04-backend-services-and-remaining-api-files/04-RESEARCH.md
@.planning/phases/04-backend-services-and-remaining-api-files/04-03-SUMMARY.md

<interfaces>
<!-- Service layer migration pattern (NEW — Pattern 3 from RESEARCH.md) -->

Service layer writes to DB error_message field:
```python
import json

# Static key (no params):
job.error_message = json.dumps({"key": "backend.errors.service.containerRestartedDuringBackup"})

# Parameterized:
job.error_message = json.dumps({
    "key": "backend.errors.service.failedCreateDestinationDir",
    "params": {"error": str(e)}
})

# Warning append (process_utils.py) — new line after existing content:
job.error_message += "\n" + json.dumps({"key": "backend.errors.service.warningFailedBreakLock"})
```

CRITICAL: Never touch error_parts.append(f"LOCK_ERROR::{repository}") — regex in BackupJobsTable.tsx:331 depends on exact format.
CRITICAL: Never migrate format_error_message() output — raw borg CLI exit code descriptions, excluded per PROJECT.md.

ErrorDetailsDialog.tsx pattern (Pattern 5 from RESEARCH.md):
```tsx
// Add import at top of file:
import { translateBackendKey } from '../utils/translateBackendKey'

// Change line 54 from:
{job.error_message}
// To:
{job.error_message
  .split('\n')
  .map(line => translateBackendKey(line))
  .join('\n')}
```

Why line-by-line: Shape 2 JSON matching only works per-line. LOCK_ERROR:: lines fall through to Shape 4 (raw). Borg output lines fall through to Shape 4 (raw). Only JSON-encoded key lines get translated. Correct rendering for all content types.

New locale domain: backend.errors.service (10 keys):
  backupCompletedWithWarning (params: {exitCode})
  backupWarningPostHooksFailed (params: {failed, total})
  containerRestartedDuringBackup
  containerRestartedDuringOperation
  containerRestartedDuringRestore
  failedCreateDestinationDir (params: {error})
  failedPrepareSourcePaths
  postBackupHooksFailed (params: {failed, total})
  preBackupHooksFailed (params: {failed, total})
  restoreCompletedWithWarnings (params: {exitCode})
  unsupportedExecutionMode (params: {mode})
  warningFailedBreakLock
  warningRemoteProcessMayBeRunning
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend.errors.service locale domain to all three locale files</name>
  <files>frontend/src/locales/en.json, frontend/src/locales/es.json, frontend/src/locales/de.json</files>
  <action>
Add a new `service` domain under `backend.errors` in all three locale files. es.json and de.json use English placeholder values. Insert alphabetically among existing error domains.

New backend.errors.service keys (insert in alphabetical order):
  "backupCompletedWithWarning": "Backup completed with warning (exit code {{exitCode}})"
  "backupWarningPostHooksFailed": "Backup succeeded with warning but post-backup hooks failed: {{failed}}/{{total}} scripts failed"
  "containerRestartedDuringBackup": "Container restarted during backup operation"
  "containerRestartedDuringOperation": "Container restarted during operation"
  "containerRestartedDuringRestore": "Container restarted during restore operation"
  "failedCreateDestinationDir": "Failed to create destination directory: {{error}}"
  "failedPrepareSourcePaths": "Failed to prepare source paths: all SSH mounts failed or no valid paths"
  "postBackupHooksFailed": "Backup succeeded but post-backup hooks failed: {{failed}}/{{total}} scripts failed"
  "preBackupHooksFailed": "Pre-backup hooks failed: {{failed}}/{{total}} scripts failed"
  "restoreCompletedWithWarnings": "Restore completed with warnings (exit code {{exitCode}})"
  "unsupportedExecutionMode": "Unsupported execution mode: {{mode}}"
  "warningFailedBreakLock": "(Warning: Failed to automatically break lock)"
  "warningRemoteProcessMayBeRunning": "(Warning: Remote process may still be running, manual verification recommended)"

Verify JSON validity after editing each file. All 13 keys must appear identically in all three files.
  </action>
  <verify>
    <automated>python3 -c "
import json
service_keys = ['backupCompletedWithWarning','backupWarningPostHooksFailed','containerRestartedDuringBackup','containerRestartedDuringOperation','containerRestartedDuringRestore','failedCreateDestinationDir','failedPrepareSourcePaths','postBackupHooksFailed','preBackupHooksFailed','restoreCompletedWithWarnings','unsupportedExecutionMode','warningFailedBreakLock','warningRemoteProcessMayBeRunning']
for f in ['frontend/src/locales/en.json','frontend/src/locales/es.json','frontend/src/locales/de.json']:
    d = json.load(open(f))
    e = d['backend']['errors']
    assert 'service' in e, f'{f}: missing backend.errors.service domain'
    for k in service_keys:
        assert k in e['service'], f'{f}: missing service.{k}'
print('All 13 service keys present in all 3 locale files')
"</automated>
  </verify>
  <done>backend.errors.service domain with 13 keys exists in all three locale files with identical structure. JSON valid.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate service layer files and update ErrorDetailsDialog</name>
  <files>app/services/backup_service.py, app/services/restore_service.py, app/utils/process_utils.py, frontend/src/components/ErrorDetailsDialog.tsx</files>
  <action>
First, check if `import json` exists at the top of each service file. Add it if missing.

**backup_service.py** (SVC-01, per RESEARCH.md backup_service.py section):

Line ~1399 area — local variable `error_msg` assignment before line 1405:
Before: `error_msg = f"Pre-backup hooks failed: {n}/{m} scripts failed"`
After:  `error_msg = json.dumps({"key": "backend.errors.service.preBackupHooksFailed", "params": {"failed": n, "total": m}})`
(Line 1405 `job.error_message = error_msg` stays unchanged — it already uses the variable.)

Line ~1446:
Before: `job.error_message = "Failed to prepare source paths: all SSH mounts failed or no valid paths"`
After:  `job.error_message = json.dumps({"key": "backend.errors.service.failedPrepareSourcePaths"})`

Line ~1897 area — local variable or direct assignment for post-backup hooks:
Before: `... = f"Backup succeeded but post-backup hooks failed: {n}/{m} scripts failed"`
After:  assign `json.dumps({"key": "backend.errors.service.postBackupHooksFailed", "params": {"failed": n, "total": m}})`

Line ~1927:
Before: `... = f"Backup completed with warning (exit code {code})"`
After:  `json.dumps({"key": "backend.errors.service.backupCompletedWithWarning", "params": {"exitCode": code}})`

Line ~1962 area — hooks + warning combination:
Before: `... = f"Backup succeeded with warning but post-backup hooks failed: {n}/{m} scripts failed"`
After:  `json.dumps({"key": "backend.errors.service.backupWarningPostHooksFailed", "params": {"failed": n, "total": m}})`

Line ~2022: `job.error_message = "\n".join(error_parts)` — DO NOT CHANGE. error_parts contains LOCK_ERROR:: sentinel and borg format_error_message() output. The hook failure messages above are set BEFORE this line, in separate error paths.

Line ~2190: `job.error_message = str(e)` — EXCLUDE. Raw Python exception from outer try/catch.

**restore_service.py** (SVC-02, per RESEARCH.md restore_service.py section):

Line ~81:
Before: `... = f"Unsupported execution mode: {execution_mode}"`
After:  `json.dumps({"key": "backend.errors.service.unsupportedExecutionMode", "params": {"mode": execution_mode}})`

Line ~140:
Before: `... = f"Failed to create destination directory: {str(e)}"`
After:  `json.dumps({"key": "backend.errors.service.failedCreateDestinationDir", "params": {"error": str(e)}})`

Line ~422:
Before: `... = f"Restore completed with warnings (exit code {code})"`
After:  `json.dumps({"key": "backend.errors.service.restoreCompletedWithWarnings", "params": {"exitCode": code}})`

Line ~818 (second site of same string with exit code 1):
Before: `... = f"Restore completed with warnings (exit code 1)"`
After:  `json.dumps({"key": "backend.errors.service.restoreCompletedWithWarnings", "params": {"exitCode": 1}})`

EXCLUDE (all of these — raw exception or borg output):
- Line ~393: composite borg output string
- Line ~443: stderr_output or process exit code
- Line ~466: str(e)
- Line ~480: str(e)
- Line ~849: str(e)

**process_utils.py** (SVC-03, per RESEARCH.md process_utils.py section):

Lines ~163, 174, 186, 227 — direct job.error_message assignments:
Before: `job.error_message = "Container restarted during backup operation"` (line 163)
After:  `job.error_message = json.dumps({"key": "backend.errors.service.containerRestartedDuringBackup"})`

Before: `job.error_message = "Container restarted during restore operation"` (line 174)
After:  `job.error_message = json.dumps({"key": "backend.errors.service.containerRestartedDuringRestore"})`

Before: `job.error_message = "Container restarted during operation"` (lines 186 and 227)
After:  `job.error_message = json.dumps({"key": "backend.errors.service.containerRestartedDuringOperation"})`

Lines ~210, 251 — warning APPEND (new line pattern):
Before: `job.error_message += " (Warning: Failed to automatically break lock)"`
After:  `job.error_message += "\n" + json.dumps({"key": "backend.errors.service.warningFailedBreakLock"})`

Lines ~215, 256 — warning APPEND (new line pattern):
Before: `job.error_message += " (Warning: Remote process may still be running, manual verification recommended)"`
After:  `job.error_message += "\n" + json.dumps({"key": "backend.errors.service.warningRemoteProcessMayBeRunning"})`

**ErrorDetailsDialog.tsx** (frontend — minimal change):

Read the file first to confirm line 54 content. Add import at top:
```tsx
import { translateBackendKey } from '../utils/translateBackendKey'
```

Change line 54 rendering from:
```tsx
{job.error_message}
```
To:
```tsx
{job.error_message
  .split('\n')
  .map(line => translateBackendKey(line))
  .join('\n')}
```

This renders the dialog with: LOCK_ERROR:: lines pass through as raw text (Shape 4 fallback), JSON key strings get translated (Shape 2), borg output lines pass through as raw text (Shape 4).

After all Python changes, run syntax checks. Run TypeScript compiler check for the frontend change.
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys
for f in ['app/services/backup_service.py','app/services/restore_service.py','app/utils/process_utils.py']:
    with open(f) as fp:
        src = fp.read()
    assert 'import json' in src or 'from json' in src, f'{f}: missing json import'
    try:
        ast.parse(src)
        print(f'{f}: syntax OK, json import present')
    except SyntaxError as e:
        print(f'{f}: SYNTAX ERROR: {e}')
        sys.exit(1)
" && python3 -c "
import subprocess
result = subprocess.run(['grep', '-n', 'LOCK_ERROR::', 'app/services/backup_service.py'], capture_output=True, text=True)
lines = result.stdout.splitlines()
for l in lines:
    if 'json.dumps' in l:
        print('ERROR: LOCK_ERROR:: sentinel may have been JSON-encoded:', l)
    else:
        print('LOCK_ERROR:: sentinel intact:', l)
" && grep -n 'translateBackendKey' frontend/src/components/ErrorDetailsDialog.tsx</automated>
  </verify>
  <done>backup_service.py, restore_service.py, and process_utils.py all have json import and pass Python syntax check. LOCK_ERROR:: sentinel line in backup_service.py is NOT json.dumps-encoded. ErrorDetailsDialog.tsx imports translateBackendKey and renders job.error_message line-by-line through the utility.</done>
</task>

</tasks>

<verification>
1. JSON validity + service domain: run Task 1 automated check
2. Python syntax: `python3 -m py_compile app/services/backup_service.py app/services/restore_service.py app/utils/process_utils.py`
3. LOCK_ERROR:: sentinel preserved: `grep 'LOCK_ERROR::' app/services/backup_service.py` — must show raw f-string, not json.dumps
4. json import present: `grep 'import json' app/services/backup_service.py app/services/restore_service.py app/utils/process_utils.py`
5. ErrorDetailsDialog updated: `grep 'translateBackendKey' frontend/src/components/ErrorDetailsDialog.tsx`
6. TypeScript compiles: `cd frontend && npx tsc --noEmit 2>&1 | head -20`
</verification>

<success_criteria>
- SVC-01: backup_service.py error_message writes use json.dumps({"key": "backend.errors.service.*"}) format
- SVC-02: restore_service.py error_message writes use json.dumps({"key": "backend.errors.service.*"}) format
- SVC-03: process_utils.py container restart strings use json.dumps; warning appends use newline + json.dumps pattern
- LOCK_ERROR:: sentinel preserved as raw string (not json.dumps encoded)
- ErrorDetailsDialog.tsx renders job.error_message through translateBackendKey line-by-line
- backend.errors.service domain with 13 keys in all 3 locale files
- Python syntax check passes for all 3 service files
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-services-and-remaining-api-files/04-04-SUMMARY.md`
</output>
