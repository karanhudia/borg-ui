---
phase: 04-backend-services-and-remaining-api-files
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/api/ssh_keys.py
  - app/api/settings.py
  - frontend/src/locales/en.json
  - frontend/src/locales/es.json
  - frontend/src/locales/de.json
autonomous: true
requirements: [BKND-06]

must_haves:
  truths:
    - "SSH key API errors (invalid format, key name exists, no system key, failed generate) display translated text in the frontend"
    - "Settings and user management API errors (admin required, username exists, email exists, user not found, cannot delete last admin) display translated text in the frontend"
    - "Settings success messages (updated, user created, user deleted, password changed) display translated text in the frontend"
    - "All new keys are present in en.json, es.json, and de.json"
  artifacts:
    - path: "app/api/ssh_keys.py"
      provides: "All user-facing HTTPException.detail and message fields use translation key dict format"
    - path: "app/api/settings.py"
      provides: "All user-facing HTTPException.detail and message fields use translation key dict format"
    - path: "frontend/src/locales/en.json"
      provides: "New backend.errors.ssh additions + new backend.errors.settings domain + new backend.success.settings domain"
  key_links:
    - from: "app/api/ssh_keys.py"
      to: "frontend/src/locales/en.json"
      via: "translation key strings matching locale key paths"
      pattern: "backend\\.errors\\.ssh\\."
    - from: "app/api/settings.py"
      to: "frontend/src/locales/en.json"
      via: "translation key strings matching locale key paths"
      pattern: "backend\\.errors\\.settings\\.|backend\\.success\\.settings\\."
---

<objective>
Migrate all user-facing HTTPException.detail strings and message response fields in ssh_keys.py and settings.py (the two largest remaining API files) to translation key dict format. Introduces two new locale domains: backend.errors.settings and backend.success.settings.

Purpose: Satisfies the ssh_keys and settings/users portion of BKND-06.
Output: ssh_keys.py and settings.py fully migrated; new settings locale domain established.
</objective>

<execution_context>
@/Users/karanhudia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karanhudia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backend-services-and-remaining-api-files/04-CONTEXT.md
@.planning/phases/04-backend-services-and-remaining-api-files/04-RESEARCH.md
@.planning/phases/04-backend-services-and-remaining-api-files/04-01-SUMMARY.md

<interfaces>
<!-- Existing SSH locale keys (reuse before creating new ones) -->

Existing backend.errors.ssh keys:
  sshKeyNotFound, sshConnectionNotFound, connectionTestFailed, keyDeployFailed, keyRemoveFailed

Existing backend.success.ssh keys:
  sshKeyCreated, sshKeyDeleted, sshKeyDeployed, sshKeyRemoved, connectionCreated, connectionUpdated, connectionDeleted, connectionTestSuccess

Existing backend.errors.auth keys (reuse for cross-domain strings):
  currentPasswordIncorrect (reuse for password change in settings.py)

Existing backend.errors.repo keys (reuse for cross-domain strings):
  repositoryNotFound (reuse for cache-clear repo not found in settings.py)

Migration pattern (established):
  Static error:       raise HTTPException(status_code=404, detail={"key": "backend.errors.ssh.keyName"})
  Parameterized:      raise HTTPException(status_code=400, detail={"key": "backend.errors.ssh.keyName", "params": {"name": var}})
  Success message:    return {"message": "backend.success.settings.settingName"}

New ssh_keys.py keys to add (backend.errors.ssh):
  adminAccessRequired, keyNameAlreadyExists, invalidPublicKeyFormat, systemKeyAlreadyExists,
  invalidKeyType (params: {types}), failedGenerateKey (params: {error}), privateKeyFileNotFound (params: {path}),
  invalidPrivateKeyFormat, publicKeyFileNotFound (params: {path}), noSystemKeyFound,
  keyGeneratedButDeployFailed (params: {error}), failedCollectStorage, cannotEnableAsBackupSource (params: {error})

New ssh_keys.py keys to add (backend.success.ssh):
  storageRefreshed

New settings.py locale domain (backend.errors.settings — new domain):
  adminAccessRequired, invalidLogSavePolicy (params: {policies}), logSizeTooSmall,
  usernameAlreadyExists, emailAlreadyExists, userNotFound, cannotDeleteLastAdmin,
  cannotDeleteOwnAccount, atLeastOneSettingRequired, failedConnectRedis (params: {error})

New settings.py locale domain (backend.success.settings — new domain):
  systemSettingsUpdated, noRepositoriesToRefresh, statsRefreshStarted (params: {count}),
  userCreated, userUpdated, userDeleted, passwordReset, passwordChanged, profileUpdated,
  preferencesUpdated, systemCleanupCompleted, logCleanupCompleted (params: {count, sizeMb}),
  cacheCleared (params: {count, repositoryId}), allCacheCleared (params: {count}),
  cacheSettingsUpdated
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new SSH and settings locale keys to all three locale files</name>
  <files>frontend/src/locales/en.json, frontend/src/locales/es.json, frontend/src/locales/de.json</files>
  <action>
Add all new locale keys to all three locale files. es.json and de.json use English placeholder values.

**New backend.errors.ssh keys** (insert alphabetically into existing ssh domain):
  "adminAccessRequired": "Admin access required"
  "cannotEnableAsBackupSource": "Cannot enable as backup source: {{error}}"
  "failedCollectStorage": "Failed to collect storage information"
  "failedGenerateKey": "Failed to generate SSH key: {{error}}"
  "invalidKeyType": "Invalid key type. Must be one of: {{types}}"
  "invalidPrivateKeyFormat": "Invalid private key format"
  "invalidPublicKeyFormat": "Invalid public key format"
  "keyGeneratedButDeployFailed": "SSH key generated but deployment failed: {{error}}"
  "keyNameAlreadyExists": "SSH key name already exists"
  "noSystemKeyFound": "No system SSH key found"
  "privateKeyFileNotFound": "Private key file not found: {{path}}"
  "publicKeyFileNotFound": "Public key file not found: {{path}}"
  "systemKeyAlreadyExists": "System SSH key already exists"

**New backend.success.ssh keys** (insert alphabetically into existing ssh success domain):
  "storageRefreshed": "Storage information refreshed successfully"

**New backend.errors.settings domain** (create new domain block, alphabetical within):
  "adminAccessRequired": "Admin access required"
  "atLeastOneSettingRequired": "At least one setting must be provided"
  "cannotDeleteLastAdmin": "Cannot delete the last admin user"
  "cannotDeleteOwnAccount": "Cannot delete your own account"
  "emailAlreadyExists": "Email already exists"
  "failedConnectRedis": "Failed to connect to Redis: {{error}}"
  "invalidLogSavePolicy": "Invalid log_save_policy. Must be one of: {{policies}}"
  "logSizeTooSmall": "log_max_total_size_mb must be at least 10 MB"
  "userNotFound": "User not found"
  "usernameAlreadyExists": "Username already exists"

**New backend.success.settings domain** (create new domain block, alphabetical within):
  "allCacheCleared": "Cleared all cache ({{count}} entries)"
  "cacheCleared": "Cleared {{count}} cache entries for repository {{repositoryId}}"
  "cacheSettingsUpdated": "Cache settings updated successfully"
  "logCleanupCompleted": "Log cleanup completed. Deleted {{count}} files ({{sizeMb}} MB freed)"
  "noRepositoriesToRefresh": "No repositories to refresh"
  "passwordChanged": "Password changed successfully"
  "passwordReset": "Password reset successfully"
  "preferencesUpdated": "Preferences updated successfully"
  "profileUpdated": "Profile updated successfully"
  "statsRefreshStarted": "Stats refresh started for {{count}} repositories"
  "systemCleanupCompleted": "System cleanup completed successfully"
  "systemSettingsUpdated": "System settings updated successfully"
  "userCreated": "User created successfully"
  "userDeleted": "User deleted successfully"
  "userUpdated": "User updated successfully"

Verify JSON validity after editing each file. Ensure the new `settings` domain block is added inside `backend.errors` and `backend.success` at the appropriate position (alphabetical among domains).
  </action>
  <verify>
    <automated>python3 -c "
import json
for f in ['frontend/src/locales/en.json','frontend/src/locales/es.json','frontend/src/locales/de.json']:
    d = json.load(open(f))
    e = d['backend']['errors']
    s = d['backend']['success']
    ssh_new = ['adminAccessRequired','cannotEnableAsBackupSource','failedCollectStorage','failedGenerateKey','invalidKeyType','invalidPrivateKeyFormat','invalidPublicKeyFormat','keyGeneratedButDeployFailed','keyNameAlreadyExists','noSystemKeyFound','privateKeyFileNotFound','publicKeyFileNotFound','systemKeyAlreadyExists']
    for k in ssh_new:
        assert k in e['ssh'], f'{f}: missing ssh.{k}'
    assert 'storageRefreshed' in s['ssh'], f'{f}: missing success.ssh.storageRefreshed'
    assert 'settings' in e, f'{f}: missing backend.errors.settings domain'
    assert 'settings' in s, f'{f}: missing backend.success.settings domain'
    settings_err = ['adminAccessRequired','atLeastOneSettingRequired','cannotDeleteLastAdmin','cannotDeleteOwnAccount','emailAlreadyExists','failedConnectRedis','invalidLogSavePolicy','logSizeTooSmall','userNotFound','usernameAlreadyExists']
    for k in settings_err:
        assert k in e['settings'], f'{f}: missing settings.{k}'
    settings_suc = ['allCacheCleared','cacheCleared','cacheSettingsUpdated','logCleanupCompleted','noRepositoriesToRefresh','passwordChanged','passwordReset','preferencesUpdated','profileUpdated','statsRefreshStarted','systemCleanupCompleted','systemSettingsUpdated','userCreated','userDeleted','userUpdated']
    for k in settings_suc:
        assert k in s['settings'], f'{f}: missing success.settings.{k}'
print('All new SSH and settings keys present in all 3 locale files')
"</automated>
  </verify>
  <done>All new locale keys exist in all three locale files with identical key structure. JSON valid. Existing keys unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ssh_keys.py and settings.py HTTPException.detail strings to translation keys</name>
  <files>app/api/ssh_keys.py, app/api/settings.py</files>
  <action>
Migrate all user-facing HTTPException.detail and message response fields to translation key format. Follow the research migration tables from 04-RESEARCH.md exactly.

**ssh_keys.py migration** (per RESEARCH.md ssh_keys.py section):
- All `"Admin access required"` → `{"key": "backend.errors.ssh.adminAccessRequired"}` (multiple sites)
- `"SSH key name already exists"` sites → `{"key": "backend.errors.ssh.keyNameAlreadyExists"}`
- `f"SSH key with name '{name}' already exists"` → `{"key": "backend.errors.ssh.keyNameAlreadyExists", "params": {"name": name}}`
- `"Invalid public key format"` → `{"key": "backend.errors.ssh.invalidPublicKeyFormat"}`
- `"System SSH key already exists..."` → `{"key": "backend.errors.ssh.systemKeyAlreadyExists"}` (generate and import)
- `f"Invalid key type. Must be one of: {types}"` → `{"key": "backend.errors.ssh.invalidKeyType", "params": {"types": types}}`
- `f"Failed to generate SSH key: {error}"` → `{"key": "backend.errors.ssh.failedGenerateKey", "params": {"error": error}}`
- `f"Private key file not found: {path}"` → `{"key": "backend.errors.ssh.privateKeyFileNotFound", "params": {"path": path}}`
- `"Invalid private key format..."` → `{"key": "backend.errors.ssh.invalidPrivateKeyFormat"}`
- `f"Public key file not found: {path}"` → `{"key": "backend.errors.ssh.publicKeyFileNotFound", "params": {"path": path}}`
- `"Invalid public key format. Must start with..."` → reuse `{"key": "backend.errors.ssh.invalidPublicKeyFormat"}`
- `"No system SSH key found..."` (×3) → `{"key": "backend.errors.ssh.noSystemKeyFound"}`
- `f"SSH key generated but deployment failed: {error}"` → `{"key": "backend.errors.ssh.keyGeneratedButDeployFailed", "params": {"error": error}}`
- `"SSH connection updated successfully"` message field → `"backend.success.ssh.connectionUpdated"` (reuse existing)
- `"Storage information refreshed successfully"` message field → `"backend.success.ssh.storageRefreshed"`
- `"Failed to collect storage information"` → `{"key": "backend.errors.ssh.failedCollectStorage"}`
- `f"Cannot enable as backup source: {error_msg}..."` → `{"key": "backend.errors.ssh.cannotEnableAsBackupSource", "params": {"error": error_msg}}`

**settings.py migration** (per RESEARCH.md settings.py section):
- All `"Admin access required"` → `{"key": "backend.errors.settings.adminAccessRequired"}` (multiple sites)
- `f"Invalid log_save_policy. Must be one of: {policies}"` → `{"key": "backend.errors.settings.invalidLogSavePolicy", "params": {"policies": policies}}`
- `"log_max_total_size_mb must be at least 10 MB"` → `{"key": "backend.errors.settings.logSizeTooSmall"}`
- `"Username already exists"` (×2) → `{"key": "backend.errors.settings.usernameAlreadyExists"}`
- `"Email already exists"` (×2) → `{"key": "backend.errors.settings.emailAlreadyExists"}`
- `"User not found"` (×3) → `{"key": "backend.errors.settings.userNotFound"}`
- `"Cannot delete the last admin user"` → `{"key": "backend.errors.settings.cannotDeleteLastAdmin"}`
- `"Cannot delete your own account"` → `{"key": "backend.errors.settings.cannotDeleteOwnAccount"}`
- `"Current password is incorrect"` → `{"key": "backend.errors.auth.currentPasswordIncorrect"}` (reuse existing auth key)
- `"At least one setting must be provided"` → `{"key": "backend.errors.settings.atLeastOneSettingRequired"}`
- `"System settings updated successfully"` message field → `"backend.success.settings.systemSettingsUpdated"`
- `"No repositories to refresh"` message field → `"backend.success.settings.noRepositoriesToRefresh"`
- `f"Stats refresh started for {n} repositories..."` message field → `"backend.success.settings.statsRefreshStarted"` (note: count is lost in bare string format; acceptable per architecture — frontend gets key only)
- `"User created successfully"` message field → `"backend.success.settings.userCreated"`
- `"User updated successfully"` message field → `"backend.success.settings.userUpdated"`
- `"User deleted successfully"` message field → `"backend.success.settings.userDeleted"`
- `"Password reset successfully"` message field → `"backend.success.settings.passwordReset"`
- `"Password changed successfully"` message field → `"backend.success.settings.passwordChanged"`
- `"Profile updated successfully"` message field → `"backend.success.settings.profileUpdated"`
- `"Preferences updated successfully"` message field → `"backend.success.settings.preferencesUpdated"`
- `"System cleanup completed successfully"` message field → `"backend.success.settings.systemCleanupCompleted"`
- `f"Log cleanup completed. Deleted {n} files..."` message field → `"backend.success.settings.logCleanupCompleted"`
- `f"Cleared {n} cache entries for repository {id}"` message field → `"backend.success.settings.cacheCleared"`
- `f"Cleared all cache ({n} entries)"` message field → `"backend.success.settings.allCacheCleared"`
- `"Cache settings updated successfully..."` message field → `"backend.success.settings.cacheSettingsUpdated"`
- `f"Repository not found"` (cache clear endpoint) → `{"key": "backend.errors.repo.repositoryNotFound"}` (reuse existing repo key)
- `f"Failed to connect to Redis: {error}"` → `{"key": "backend.errors.settings.failedConnectRedis", "params": {"error": error}}`

Note for parameterized message fields: When a message field contains dynamic values (count, sizeMb, etc.), the bare string approach loses those values. This is acceptable per the architecture decision — the frontend calls `translateBackendKey(data.message)` and gets the key string which renders with missing params showing `{{param}}` until Phase 5 wires them properly. The alternative of returning `{"key": ..., "params": {...}}` in message fields would require frontend changes beyond scope. Use bare key strings for message fields.

After all changes, run Python syntax check on both files.
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys
for f in ['app/api/ssh_keys.py', 'app/api/settings.py']:
    with open(f) as fp:
        src = fp.read()
    try:
        ast.parse(src)
        print(f'{f}: syntax OK')
    except SyntaxError as e:
        print(f'{f}: SYNTAX ERROR: {e}')
        sys.exit(1)
"</automated>
  </verify>
  <done>Both ssh_keys.py and settings.py pass Python syntax check. All user-facing HTTPException.detail and message fields use translation key format. Cross-domain key reuse applied correctly (auth.currentPasswordIncorrect, repo.repositoryNotFound).</done>
</task>

</tasks>

<verification>
1. JSON validity and key presence: run Task 1 automated check
2. Python syntax: `python3 -m py_compile app/api/ssh_keys.py app/api/settings.py`
3. No raw English strings in HTTPException.detail: `grep -n 'detail="' app/api/ssh_keys.py app/api/settings.py | grep -v "str(e)"` should return empty or only intentional exclusions
4. Key parity across 3 locale files: python3 check confirms all domains present in all files
</verification>

<success_criteria>
- BKND-06 (SSH + settings portion): ssh_keys.py and settings.py have zero user-facing raw English strings in HTTPException.detail or message response fields
- New backend.errors.settings and backend.success.settings locale domains established in all 3 locale files
- New SSH locale keys added to all 3 locale files
- Python syntax check passes for both files
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-services-and-remaining-api-files/04-02-SUMMARY.md`
</output>
