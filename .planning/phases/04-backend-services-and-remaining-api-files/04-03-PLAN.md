---
phase: 04-backend-services-and-remaining-api-files
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - app/api/archives.py
  - app/api/mounts.py
  - app/api/activity.py
  - app/api/filesystem.py
  - app/api/browse.py
  - app/api/packages.py
  - app/api/notifications.py
  - app/api/scripts.py
  - app/api/scripts_library.py
  - frontend/src/locales/en.json
  - frontend/src/locales/es.json
  - frontend/src/locales/de.json
autonomous: true
requirements: [BKND-06]

must_haves:
  truths:
    - "Archive API errors (not found, delete already running, delete job not found) display translated text in the frontend"
    - "Mount API errors (mount not found, unmount failed) display translated text in the frontend"
    - "Activity API errors (invalid job type, job not found, cannot delete running) display translated text in the frontend"
    - "Filesystem, browse, packages, notifications, scripts, and scripts_library user-facing errors display translated text"
    - "Download endpoint strings are NOT migrated (browser-navigated, excluded per architecture)"
    - "All new keys are present in en.json, es.json, and de.json"
  artifacts:
    - path: "app/api/archives.py"
      provides: "User-facing HTTPException.detail and message fields use translation key dict format; download endpoint excluded"
    - path: "app/api/mounts.py"
      provides: "User-facing HTTPException.detail and message fields use translation key dict format"
    - path: "app/api/activity.py"
      provides: "Job delete endpoint migrated; log download endpoint excluded"
    - path: "frontend/src/locales/en.json"
      provides: "New domains: archives, mounts, filesystem, browse, packages, notifications, activity, scripts; plus new keys in existing domains"
  key_links:
    - from: "app/api/archives.py"
      to: "frontend/src/locales/en.json"
      via: "translation key strings matching locale key paths"
      pattern: "backend\\.errors\\.archives\\."
    - from: "app/api/activity.py"
      to: "frontend/src/locales/en.json"
      via: "translation key strings matching locale key paths"
      pattern: "backend\\.errors\\.activity\\."
---

<objective>
Migrate all remaining API files (archives, mounts, activity, filesystem, browse, packages, notifications, scripts, scripts_library) to use translation key dict format for all user-facing HTTPException.detail and message fields. Introduces multiple new locale domains. Browser download endpoint strings are explicitly excluded per architecture decision.

Purpose: Completes BKND-06 — no backend API file returns a raw hardcoded English string in HTTPException.detail or message fields.
Output: 9 remaining API files migrated; ~8 new locale domains established.
</objective>

<execution_context>
@/Users/karanhudia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/karanhudia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backend-services-and-remaining-api-files/04-CONTEXT.md
@.planning/phases/04-backend-services-and-remaining-api-files/04-RESEARCH.md
@.planning/phases/04-backend-services-and-remaining-api-files/04-02-SUMMARY.md

<interfaces>
<!-- Existing locale keys to REUSE (do not create duplicates) -->

Existing backend.errors.restore keys (reuse):
  repositoryNotFound — use for "Repository not found" in browse.py and archives.py non-download endpoints

Existing backend.errors.ssh keys (reuse):
  sshKeyNotFound — use for "SSH key not found" in filesystem.py

Existing backend.errors.settings keys (new, from plan 04-02, reuse):
  adminAccessRequired — use for archives.py "Admin access required"

Existing backend.errors.repo keys (reuse):
  repositoryNotFound — already exists (reuse for archives.py repo not found if different domain needed, but prefer restore.repositoryNotFound for cross-domain or create archives-specific key)

Migration pattern (established):
  Static error:       raise HTTPException(status_code=404, detail={"key": "backend.errors.archives.keyName"})
  Parameterized:      raise HTTPException(status_code=400, detail={"key": "backend.errors.archives.keyName", "params": {"id": job_id}})
  Success message:    return {"message": "backend.success.archives.keyName"}

New locale domains needed (all keys listed alphabetically within domain):

backend.errors.archives:
  adminAccessRequired, deleteAlreadyRunning (params: {jobId}), deleteJobNotFound,
  failedGetArchiveContents, failedGetArchiveInfo, failedListArchives

backend.success.archives:
  deletionCancelled, deletionStarted

backend.errors.mounts:
  canOnlyUnmountBorgMounts, mountInfoNotFound, mountNotFound (params: {mountId}),
  unmountFailed (params: {mountId})

backend.errors.activity:
  adminOnlyDelete, cannotDeleteRunningJob, invalidJobType (params: {jobType}),
  jobNotFound (params: {jobType})

backend.success.activity:
  jobDeleted (params: {jobType})

backend.errors.filesystem:
  folderAlreadyExists (params: {name}), invalidConnectionType, invalidFolderName,
  pathNotDirectory (params: {path}), pathNotFound (params: {path}),
  permissionDenied (params: {path}), permissionDeniedCreateFolder,
  sshConnectionDetailsRequired, sshConnectionTimeout, sshParamsRequired

backend.errors.browse:
  archiveMemoryTooHigh (params: {estimatedMb, maxMb}), archiveTooLarge (params: {linesRead, maxItems})

backend.errors.packages:
  jobNotFound, packageAlreadyExists (params: {name}), packageNotFound

backend.success.packages:
  installationInProgress (params: {name}), installationStarted (params: {name}),
  packageRemoved (params: {name})

backend.errors.notifications:
  settingNotFound

backend.errors.scripts (if scripts.py has user-facing strings — check file; may be empty or minimal):
  (determined by executor after reading scripts.py and scripts_library.py)

Exclusions that MUST NOT be migrated:
  - archives.py download_file_from_archive() — browser download, FileResponse, token-based auth
  - activity.py download_job_logs() — browser download, FileResponse, token-based auth
  - All str(e) catch-all exception strings — raw Python exceptions
  - f"Failed to list remote directory..." and similar raw OS/SSH error strings in filesystem.py
  - f"Failed to browse archive: {str(e)}" in browse.py
  - f"Failed to start installation: {str(e)}" in packages.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new locale keys for all remaining API domains to all three locale files</name>
  <files>frontend/src/locales/en.json, frontend/src/locales/es.json, frontend/src/locales/de.json</files>
  <action>
First, read scripts.py and scripts_library.py to identify any user-facing HTTPException.detail strings that need locale keys. Then add all new locale keys to all three locale files.

Add the following new domains and keys (insert new domains alphabetically among existing domains; keys alphabetically within each domain). es.json and de.json use English placeholder values.

**backend.errors.activity** (new domain):
  "adminOnlyDelete": "Only administrators can delete job entries"
  "cannotDeleteRunningJob": "Cannot delete running job. Cancel it first or wait for completion."
  "invalidJobType": "Invalid job type: {{jobType}}"
  "jobNotFound": "{{jobType}} job not found"

**backend.success.activity** (new domain):
  "jobDeleted": "{{jobType}} job deleted successfully"

**backend.errors.archives** (new domain):
  "adminAccessRequired": "Admin access required"
  "deleteAlreadyRunning": "Delete operation is already running for this archive (Job ID: {{jobId}})"
  "deleteJobNotFound": "Delete job not found"
  "failedGetArchiveContents": "Failed to get archive contents"
  "failedGetArchiveInfo": "Failed to get archive info"
  "failedListArchives": "Failed to list archives"

**backend.success.archives** (new domain):
  "deletionCancelled": "Delete job cancelled successfully"
  "deletionStarted": "Archive deletion started in background"

**backend.errors.browse** (new domain):
  "archiveMemoryTooHigh": "Archive memory too high: {{estimatedMb}} MB estimated (max {{maxMb}} MB)"
  "archiveTooLarge": "Archive too large: {{linesRead}} items read (max {{maxItems}})"

**backend.errors.filesystem** (new domain):
  "folderAlreadyExists": "Folder '{{name}}' already exists"
  "invalidConnectionType": "Invalid connection type"
  "invalidFolderName": "Invalid folder name"
  "pathNotDirectory": "Path is not a directory: {{path}}"
  "pathNotFound": "Path not found: {{path}}"
  "permissionDenied": "Permission denied: {{path}}"
  "permissionDeniedCreateFolder": "Permission denied to create folder"
  "sshConnectionDetailsRequired": "SSH connection details required"
  "sshConnectionTimeout": "SSH connection timeout"
  "sshParamsRequired": "SSH key ID, host, and username are required for SSH browsing"

**backend.errors.mounts** (new domain):
  "canOnlyUnmountBorgMounts": "Can only unmount Borg archive mounts via this endpoint"
  "mountInfoNotFound": "Mount succeeded but info not found"
  "mountNotFound": "Mount {{mountId}} not found"
  "unmountFailed": "Failed to unmount {{mountId}}"

**backend.errors.notifications** (new domain):
  "settingNotFound": "Notification setting not found"

**backend.errors.packages** (new domain):
  "jobNotFound": "Job not found"
  "packageAlreadyExists": "Package '{{name}}' already exists"
  "packageNotFound": "Package not found"

**backend.success.packages** (new domain):
  "installationInProgress": "Package '{{name}}' installation already in progress"
  "installationStarted": "Package '{{name}}' installation started"
  "packageRemoved": "Package '{{name}}' removed from list"

For scripts.py and scripts_library.py: read both files first. If there are user-facing HTTPException.detail strings, add them under backend.errors.scripts and/or backend.errors.scriptsLibrary domains. If the files have no user-facing strings or only str(e) exclusions, no new keys are needed for those files.

Verify JSON validity after editing each file. All new keys must appear identically in all three files.
  </action>
  <verify>
    <automated>python3 -c "
import json
for f in ['frontend/src/locales/en.json','frontend/src/locales/es.json','frontend/src/locales/de.json']:
    d = json.load(open(f))
    e = d['backend']['errors']
    s = d['backend']['success']
    required_domains_err = ['activity','archives','browse','filesystem','mounts','notifications','packages']
    required_domains_suc = ['activity','archives','packages']
    for dom in required_domains_err:
        assert dom in e, f'{f}: missing backend.errors.{dom}'
    for dom in required_domains_suc:
        assert dom in s, f'{f}: missing backend.success.{dom}'
    activity_err = ['adminOnlyDelete','cannotDeleteRunningJob','invalidJobType','jobNotFound']
    for k in activity_err:
        assert k in e['activity'], f'{f}: missing activity.{k}'
    archives_err = ['adminAccessRequired','deleteAlreadyRunning','deleteJobNotFound','failedGetArchiveContents','failedGetArchiveInfo','failedListArchives']
    for k in archives_err:
        assert k in e['archives'], f'{f}: missing archives.{k}'
    mounts_err = ['canOnlyUnmountBorgMounts','mountInfoNotFound','mountNotFound','unmountFailed']
    for k in mounts_err:
        assert k in e['mounts'], f'{f}: missing mounts.{k}'
print('All required new domain keys present in all 3 locale files')
"</automated>
  </verify>
  <done>All new locale domains and keys exist in all three locale files with identical structure. JSON valid. Existing keys unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining API files to translation key format</name>
  <files>app/api/archives.py, app/api/mounts.py, app/api/activity.py, app/api/filesystem.py, app/api/browse.py, app/api/packages.py, app/api/notifications.py, app/api/scripts.py, app/api/scripts_library.py</files>
  <action>
Migrate all user-facing HTTPException.detail and message response fields in each file. Read each file first to confirm line numbers, then apply changes per the research tables.

**archives.py** (per RESEARCH.md archives section):
INCLUDE (migrate):
- `"Repository not found"` at lines 35, 76, 197, 241 (axios-intercepted endpoints only) → `{"key": "backend.errors.restore.repositoryNotFound"}` (reuse existing restore key)
- `"Failed to list archives"` → `{"key": "backend.errors.archives.failedListArchives"}` (not the f-string with stderr — that stays excluded)
- `"Failed to get archive info"` → `{"key": "backend.errors.archives.failedGetArchiveInfo"}`
- `"Failed to get archive contents"` → `{"key": "backend.errors.archives.failedGetArchiveContents"}`
- `"Admin access required"` → `{"key": "backend.errors.archives.adminAccessRequired"}`
- `f"Delete operation is already running for this archive (Job ID: {running_job.id})"` → `{"key": "backend.errors.archives.deleteAlreadyRunning", "params": {"jobId": running_job.id}}`
- `"Archive deletion started in background"` message field → `"backend.success.archives.deletionStarted"`
- `"Delete job not found"` → `{"key": "backend.errors.archives.deleteJobNotFound"}`
- `"Delete job cancelled successfully"` message field → `"backend.success.archives.deletionCancelled"`
EXCLUDE (do NOT migrate):
- All strings inside download_file_from_archive() function — browser download handler
- f-strings with stderr content (raw borg output): `f"Failed to list archives: {result['stderr']}"`, etc.
- str(e) catch-all exceptions

**mounts.py** (per RESEARCH.md mounts section):
- `"Mount succeeded but info not found"` → `{"key": "backend.errors.mounts.mountInfoNotFound"}`
- `f"Mount {mount_id} not found"` (×3) → `{"key": "backend.errors.mounts.mountNotFound", "params": {"mountId": mount_id}}`
- `"Can only unmount Borg archive mounts via this endpoint"` → `{"key": "backend.errors.mounts.canOnlyUnmountBorgMounts"}`
- `f"Failed to unmount {mount_id}"` → `{"key": "backend.errors.mounts.unmountFailed", "params": {"mountId": mount_id}}`
EXCLUDE: str(e) catch-all exceptions

**activity.py** (per RESEARCH.md activity section):
INCLUDE (axios-intercepted delete endpoint only):
- `f"Invalid job type: {job_type}"` (×3) → `{"key": "backend.errors.activity.invalidJobType", "params": {"jobType": job_type}}`
- `f"{job_type.capitalize()} job not found"` (×3) → `{"key": "backend.errors.activity.jobNotFound", "params": {"jobType": job_type.capitalize()}}`
- `"Only administrators can delete job entries"` → `{"key": "backend.errors.activity.adminOnlyDelete"}`
- `"Cannot delete running job. Cancel it first or wait for completion."` → `{"key": "backend.errors.activity.cannotDeleteRunningJob"}`
- `f"{job_type.capitalize()} job deleted successfully"` message field → `"backend.success.activity.jobDeleted"` (note: jobType value lost in bare string format; acceptable)
EXCLUDE: all strings in download_job_logs() function (lines 477-561 approximately) — browser download handler

**filesystem.py** (per RESEARCH.md filesystem section):
- `"SSH key ID, host, and username are required for SSH browsing"` → `{"key": "backend.errors.filesystem.sshParamsRequired"}`
- `"Invalid connection type"` → `{"key": "backend.errors.filesystem.invalidConnectionType"}`
- `f"Path not found: {path}"` → `{"key": "backend.errors.filesystem.pathNotFound", "params": {"path": path}}`
- `f"Path is not a directory: {path}"` → `{"key": "backend.errors.filesystem.pathNotDirectory", "params": {"path": path}}`
- `f"Permission denied: {path}"` → `{"key": "backend.errors.filesystem.permissionDenied", "params": {"path": path}}`
- `"SSH key not found"` → `{"key": "backend.errors.ssh.sshKeyNotFound"}` (reuse existing)
- `"SSH connection timeout"` → `{"key": "backend.errors.filesystem.sshConnectionTimeout"}`
- `"Invalid folder name"` → `{"key": "backend.errors.filesystem.invalidFolderName"}`
- `f"Folder '{name}' already exists"` → `{"key": "backend.errors.filesystem.folderAlreadyExists", "params": {"name": name}}`
- `"Permission denied to create folder"` → `{"key": "backend.errors.filesystem.permissionDeniedCreateFolder"}`
- `"SSH connection details required"` → `{"key": "backend.errors.filesystem.sshConnectionDetailsRequired"}`
EXCLUDE: `f"Failed to list remote directory '{path}': {error_msg or '...'}"` — raw OS/SSH output
EXCLUDE: `f"Failed to browse filesystem: {str(e)}"`, `f"Failed to validate path: {str(e)}"`, `f"Failed to browse remote filesystem: {str(e) or 'Unknown error'}"` — raw exceptions

**browse.py** (per RESEARCH.md browse section):
- `"Repository not found"` → `{"key": "backend.errors.restore.repositoryNotFound"}` (reuse existing)
- archive-too-large message → `{"key": "backend.errors.browse.archiveTooLarge", "params": {"linesRead": lines_read, "maxItems": max_items}}` (read actual variable names from file)
- archive-memory message → `{"key": "backend.errors.browse.archiveMemoryTooHigh", "params": {"estimatedMb": estimated_mb, "maxMb": max_mb}}` (read actual variable names from file)
EXCLUDE: `f"Failed to browse archive: {str(e)}"` — raw exception

**packages.py** (per RESEARCH.md packages section):
- `"Admin access required"` → `{"key": "backend.errors.settings.adminAccessRequired"}` (reuse existing settings key)
- `f"Package '{name}' already exists"` → `{"key": "backend.errors.packages.packageAlreadyExists", "params": {"name": name}}`
- `"Package not found"` → `{"key": "backend.errors.packages.packageNotFound"}`
- `"Job not found"` → `{"key": "backend.errors.packages.jobNotFound"}`
- `f"Package '{name}' installation already in progress"` message field → `"backend.success.packages.installationInProgress"` (note: name lost in bare string)
- `f"Package '{name}' installation started"` message field → `"backend.success.packages.installationStarted"`
- `f"Package '{name}' removed from list"` message field → `"backend.success.packages.packageRemoved"`
EXCLUDE: `f"Failed to start installation: {str(e)}"` — raw exception

**notifications.py**:
- `"Notification setting not found"` (×3) → `{"key": "backend.errors.notifications.settingNotFound"}`

**scripts.py and scripts_library.py**: Read these files. Apply migration table pattern to any user-facing HTTPException.detail strings found. Add corresponding locale keys under backend.errors.scripts and/or backend.errors.scriptsLibrary if needed. If only str(e) catch-alls exist, no migration needed.

After all changes, run Python syntax check on all 9 files.
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys
files = ['app/api/archives.py','app/api/mounts.py','app/api/activity.py','app/api/filesystem.py','app/api/browse.py','app/api/packages.py','app/api/notifications.py','app/api/scripts.py','app/api/scripts_library.py']
for f in files:
    with open(f) as fp:
        src = fp.read()
    try:
        ast.parse(src)
        print(f'{f}: syntax OK')
    except SyntaxError as e:
        print(f'{f}: SYNTAX ERROR: {e}')
        sys.exit(1)
print('All 9 files pass syntax check')
"</automated>
  </verify>
  <done>All 9 remaining API files pass Python syntax check. User-facing HTTPException.detail and message fields use translation key dict format. Download endpoint strings are excluded. str(e) catch-all exclusions confirmed untouched.</done>
</task>

</tasks>

<verification>
1. JSON validity: `python3 -c "import json; [json.load(open(f)) for f in ['frontend/src/locales/en.json','frontend/src/locales/es.json','frontend/src/locales/de.json']]; print('valid')"`
2. Key domains present: run Task 1 automated check
3. Python syntax: `python3 -m py_compile app/api/archives.py app/api/mounts.py app/api/activity.py app/api/filesystem.py app/api/browse.py app/api/packages.py app/api/notifications.py app/api/scripts.py app/api/scripts_library.py`
4. Download endpoints excluded: confirm download_file_from_archive() and download_job_logs() have NOT been modified
5. No raw English strings in HTTPException.detail (excluding intentional str(e) exclusions)
</verification>

<success_criteria>
- BKND-06 complete: all remaining API files (archives, mounts, activity, filesystem, browse, packages, notifications, scripts, scripts_library, ssh_keys from plan 02, settings from plan 02) have zero user-facing raw English strings in HTTPException.detail or message response fields
- New locale domains established for all new API domains in all 3 locale files
- Download endpoint exclusions confirmed intact
- Python syntax check passes for all 9 files
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-services-and-remaining-api-files/04-03-SUMMARY.md`
</output>
